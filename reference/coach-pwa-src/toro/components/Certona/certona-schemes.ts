import { CertonaSchemeType } from 'store/certona-schemes.atoms'

/**
 * Certona recommendation scheme constants
 * These constants define the scheme types used for different recommendation components
 */

const BECAUSE_YOU_VIEWED_RECOMMENDER_SCHEME: CertonaSchemeType = 'sitevisit1_rr'
const GONE_VIRAL_RECOMMENDER_SCHEME: CertonaSchemeType = 'sitewide1_rr'
const DEALS_SCHEME: CertonaSchemeType = 'productlisting7_rr'

export { BECAUSE_YOU_VIEWED_RECOMMENDER_SCHEME, GONE_VIRAL_RECOMMENDER_SCHEME, DEALS_SCHEME }
