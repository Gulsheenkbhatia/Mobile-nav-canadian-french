import { DrawerCloseButton as ChakraUIDrawerCloseButton } from '@chakra-ui/react'

const DrawerCloseButton: typeof ChakraUIDrawerCloseButton = (props) => {
  return <ChakraUIDrawerCloseButton {...props} />
}

export default DrawerCloseButton
