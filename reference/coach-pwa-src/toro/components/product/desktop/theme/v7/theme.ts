/**
 * PDP modern desktop theme.
 * Base structure for modern template styling.
 */
const themeModern = {
  components: {
    TemplateContainer: {
      baseStyle: {
        // empty for now
      },
    },
  },
}

export default themeModern
