import { ModalOverlay as ChakraUIModalOverlay, ModalOverlayProps } from '@chakra-ui/react'

export default function ModalOverlay(props: ModalOverlayProps) {
  return <ChakraUIModalOverlay {...props} />
}
