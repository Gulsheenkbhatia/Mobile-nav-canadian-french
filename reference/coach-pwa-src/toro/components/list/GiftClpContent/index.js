import LandingContent from 'toro/cms/components/LandingContent'

export default function GiftCLPContent({ slots, hasVideoContent = false }) {
  return <LandingContent slots={slots} hasVideoContent={hasVideoContent} />
}
