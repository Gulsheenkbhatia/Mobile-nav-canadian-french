import {
  SlideFade as ChakraSlideFade,
  SlideFadeProps as ChakraSlideFadeProps,
} from '@chakra-ui/react'

const SlideFade = (props: ChakraSlideFadeProps) => <ChakraSlideFade {...props} />

export default SlideFade
