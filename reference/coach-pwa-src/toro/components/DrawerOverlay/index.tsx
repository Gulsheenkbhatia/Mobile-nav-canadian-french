import { DrawerOverlay as ChakraUIDrawerOverlay } from '@chakra-ui/react'

const DrawerOverlay: typeof ChakraUIDrawerOverlay = (props) => {
  return <ChakraUIDrawerOverlay {...props} />
}

export default DrawerOverlay
