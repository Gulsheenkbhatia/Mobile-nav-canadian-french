{
  "buildCommand": "cd template && npm run build",
  "outputDirectory": "template/dist",
  "installCommand": "cd template && npm install",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
